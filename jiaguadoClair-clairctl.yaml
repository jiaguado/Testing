clair:
  port: 6060
  healthPort: 6061
  request:
    host: HOST
    headers:
      myHeader: header
  uri: http://XX.XX.XX.XX
  report:
    path: ./reports
    format: html
docker:
  insecure-registries: — “my-own-registry:5000”
